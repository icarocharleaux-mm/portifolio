# Dependencias do portfolio.
# O Streamlit Community Cloud instala este arquivo automaticamente no deploy.
# Mantenha a lista enxuta: cada pacote a mais e mais tempo de build.

streamlit>=1.50,<2.0
pandas>=2.0,<3.0
altair>=5.0,<7.0
